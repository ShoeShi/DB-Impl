class mergesort

new mergesort(filename, page, buffers){
this->filename = filename;
this->page = pageSize;
this->buffers = numBuffers;
}

pass0( filename , pages, buffers){

  openfile(x);
  while(n){

  for buffers/pages
  readline(n); sort, for pages
  put into array sized buffers/page
  allocarray;
  
  sortbuffers(); // using multithreading?

  write(n); sort, 
  print(n);

}

passN(n)
{
  openfile(x);
  tempfilename = "temp" + n"
  openfile(temp);

  //for each buffer
  while(n){
  readline(n);
  readline(n);
  allocarray;
  allocarray2;
  put into an array.
  pop first element
  compare first element
  place into new array.
  
  writeline(n);
  print(n);
  sort them.

  nextBuffer;
  }

}


